function inOrder(currentNode){
  let ordered = [];
  //let rootNode = currentNode;
}

function min(currentNode){
  
  if(currentNode.left){
    return currentNode;
  }else{
    min(currentNode.left)
  }
    
  
}